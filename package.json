{
  "name": "next-issue",
  "version": "0.5.2",
  "description": "Autonomous issue harness driven by the Claude Agent SDK",
  "keywords": [
    "github",
    "issues",
    "agent",
    "claude",
    "automation",
    "cli"
  ],
  "homepage": "https://github.com/ravshansbox/next-issue#readme",
  "bugs": "https://github.com/ravshansbox/next-issue/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ravshansbox/next-issue.git"
  },
  "license": "MIT",
  "author": "ravshansbox",
  "type": "module",
  "bin": {
    "next-issue": "dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node src/cli.mts",
    "test": "node --test \"test/**/*.test.mts\"",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build",
    "prepublishOnly": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.241"
  }
}
